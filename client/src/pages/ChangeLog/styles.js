import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => {
  return {
    section: {
      padding: "60px 0",
      minHeight: "100vh",
      width: "100%",
      display: "flex",
      position: "relative",
    },
  };
});

export default useStyles;
